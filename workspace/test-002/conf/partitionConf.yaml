--- # Used when producing input stores with GbintToIndexedStore driver.
scaleMode: NUMBER_PARTITIONS                          # {NUMBER_PARTITIONS, RECORDS_PER_PARTITION}
scale: 1000                                           # Value corresponding to 'scaleMode'.
